class Solution {
    public int mostWordsFound(String[] sentences) {
        int maxWords = 0;
        for (String s : sentences) {
            // count words by splitting with space
            int words = s.split(" ").length;
            maxWords = Math.max(maxWords, words);
        }
        return maxWords;
    }
}
